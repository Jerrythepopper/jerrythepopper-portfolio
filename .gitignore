# 依賴（package.json 進版控、鎖檔與 node_modules 不進）
node_modules/
package-lock.json

# 圖片產線輸入：相機原檔體積大且非發布資產（輸出的 photos/ 才進版控）
originals/*
!originals/.gitkeep
!originals/README.md

# 工作備份
backups/

# 使用者母帶 staging 區（原檔與影片不進版控）
# 2026-08-09 修：commit 2c9302b 這行是用 Big5 位元組寫進來的（PowerShell 預設 ANSI
# codepage），git 拿 UTF-8 路徑去比對永遠比不中＝母帶其實一直沒被忽略。重寫成 UTF-8。
網站照片/
